import { getDictionary } from "@/i18n/server";
export default async function GitHubPage() { const { locale } = await getDictionary(); const ja = locale === "ja"; return <main className="mx-auto w-full max-w-4xl px-4 py-8 md:px-7 md:py-10"><p className="font-mono text-[10px] text-[var(--accent)]">PHASE 7</p><h1 className="mt-3 text-4xl font-medium">GitHub Action</h1><p className="mt-3 text-sm leading-6 text-[var(--text-secondary)]">{ja ? "Checkout済みの公開WebプロジェクトからHTMLをAURELIS APIへ送信します。既定ではPRをBlockingしません。" : "Submit checked-out HTML from a public web project to the AURELIS API. The workflow is non-blocking by default."}</p><section className="panel-flat mt-7 p-5"><h2 className="font-medium">{ja ? "必要な設定" : "Required configuration"}</h2><ol className="mt-4 list-decimal space-y-2 pl-5 text-sm leading-6 text-[var(--text-secondary)]"><li><code>AURELIS_API_URL</code> {ja ? "Repository Secretを設定" : "repository secret"}</li><li>{ja ? "評価対象HTMLのPathを指定（既定 index.html）" : "Set the HTML path (default: index.html)"}</li><li>{ja ? "必要な場合のみ continue-on-error を false に変更" : "Change continue-on-error to false only when you intend to gate PRs"}</li></ol></section><p className="mt-4 text-xs text-[var(--warning)]">{ja ? "Private repository GitHub AppとStatus Check書き込みは延期されています。" : "Private-repository GitHub App and status-check writeback are deferred."}</p></main>; }
