import { getDictionary } from "@/i18n/server";
export default async function PrivacyPage() { const { locale } = await getDictionary(); const ja = locale === "ja"; const sections = ja ? [["分析対象", "入力URLまたはHTML、CSS、JavaScript、Brand Profile、Reference Corpus。貼り付けたJavaScriptは実行しません。"], ["保存データ", "入力、Hash、Raw tool result、Score、Evidence、Recommendation、Version metadata。ユーザーが削除するまで保存します。"], ["使用モデル", "環境設定されたOpenAI model ID。API Keyがない場合、AI評価はUnavailableです。"], ["削除", "各評価Reportの「評価を削除」から関連する技術・ブランド結果、Evidence、Recommendation、Job、VersionをCascade削除します。"]] : [["What is analyzed", "Submitted URLs or HTML, CSS, JavaScript, brand profiles, and reference corpora. Pasted JavaScript is not executed."], ["What is stored", "Inputs, hashes, raw tool results, scores, evidence, recommendations, and version metadata are retained until you delete them."], ["Which AI model is used", "The configured OpenAI model ID. Without an API key, AI evaluation is unavailable."], ["Deletion", "Delete an evaluation from its report to cascade-delete its technical and brand results, evidence, recommendations, job, and version metadata."]]; return <main className="mx-auto w-full max-w-4xl px-4 py-8 md:px-7 md:py-10"><p className="font-mono text-[10px] text-[var(--accent)]">PHASE 7</p><h1 className="mt-3 text-4xl font-medium">{ja ? "プライバシーとデータ" : "Privacy and data"}</h1><div className="mt-7 space-y-4">{sections.map(([title, body]) => <section className="panel-flat p-5" key={title}><h2 className="font-medium">{title}</h2><p className="mt-3 text-sm leading-7 text-[var(--text-secondary)]">{body}</p></section>)}</div></main>; }
